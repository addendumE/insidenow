
# insidenow
## supported console commads:
* address [0...MAX_NODES] -> set node address or gateway mode [set address < 0]
* tx [HEXSTRING] -> trasmit hex payload
* reboot

In gateway mode uart is enabled, now rx thread is disabled
In node mode  uart is disabled, now rx thread is enabled

